# Archived CV site

The previous CV implementation, kept as a backup. Not part of the build:
`tsconfig.app.json` only includes `src`, and `eslint.config.js` ignores this folder.

To restore it, swap this folder back to `src`.

import React from 'react';
import './education.scss';

const Education = React.forwardRef((props, ref) => {
    return <div className="education" ref={ref}>Education</div>;
});

export default Education;